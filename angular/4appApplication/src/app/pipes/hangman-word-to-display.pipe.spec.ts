import { HangmanWordToDisplayPipe } from './hangman-word-to-display.pipe';

describe('HangmanWordToDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new HangmanWordToDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
