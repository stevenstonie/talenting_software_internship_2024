import { HangmanDirective } from './hangman.directive';

describe('HangmanDirective', () => {
  it('should create an instance', () => {
    const directive = new HangmanDirective();
    expect(directive).toBeTruthy();
  });
});
