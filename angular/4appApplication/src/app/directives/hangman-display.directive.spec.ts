import { HangmanDisplayDirective } from './hangman-display.directive';

describe('HangmanDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new HangmanDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
